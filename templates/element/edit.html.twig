{% extends 'base.html.twig' %}

{% block title %}Edit Element{% endblock %}

{% block content %}

    <h1>Edit Element</h1>

    {#{% form_theme form 'element/form-template.html.twig' %}

    <div class="form-edit">
        {{ form_start(form) }}

        {{ form_end(form) }}
    </div>#}

    {{ include('element/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('backlog_show', {'id':element.backlog.id}) }}">Retour au backlog</a>

    {{ include('element/_delete_form.html.twig') }}

{% endblock %}
